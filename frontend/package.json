{
  "name": "log-viewer-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist/assets && cp index.html logs.html flow.html dist/ && cp styles.css dist/assets/ && tsc"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
